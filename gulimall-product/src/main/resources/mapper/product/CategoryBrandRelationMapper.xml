<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gulimall.product.mapper.CategoryBrandRelationMapper">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.gulimall.product.entity.CategoryBrandRelationEntity" id="categoryBrandRelationMap">
        <result property="id" column="id"/>
        <result property="brandId" column="brand_id"/>
        <result property="catelogId" column="catelog_id"/>
        <result property="catelogName" column="catelog_name"/>
    </resultMap>

    <select id="getRelationCategoryList" resultType="com.gulimall.product.entity.CategoryBrandRelationEntity">
        SELECT
            t1.id, t1.catelog_id, t1.brand_id, t2.name AS catelogName
        FROM
            pms_category_brand_relation AS t1
            LEFT JOIN pms_category AS t2 ON t1.catelog_id = t2.cat_id
        WHERE
            t1.brand_id = #{brandId}
    </select>


    <select id="getBrandsByCategory" resultType="com.gulimall.product.entity.BrandEntity">
        SELECT
            t1.*
        FROM
            pms_brand AS t1
            LEFT JOIN pms_category_brand_relation AS t2 ON t1.brand_id = t2.brand_id
        <where>
            <if test="categoryId != 0 and categoryId != null">
                t2.catelog_id = #{categoryId}
            </if>
        </where>
    </select>

</mapper>
