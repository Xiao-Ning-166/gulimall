<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gulimall.product.mapper.SkuInfoMapper">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.gulimall.product.entity.SkuInfoEntity" id="skuInfoMap">
        <result property="skuId" column="sku_id"/>
        <result property="spuId" column="spu_id"/>
        <result property="skuName" column="sku_name"/>
        <result property="skuDesc" column="sku_desc"/>
        <result property="catalogId" column="catalog_id"/>
        <result property="brandId" column="brand_id"/>
        <result property="skuDefaultImg" column="sku_default_img"/>
        <result property="skuTitle" column="sku_title"/>
        <result property="skuSubtitle" column="sku_subtitle"/>
        <result property="price" column="price"/>
        <result property="saleCount" column="sale_count"/>
    </resultMap>

    <select id="queryPage" resultType="com.gulimall.product.vo.SkuInfoVO">
        SELECT
            t1.sku_id, t1.sku_name, t4.img_url AS sku_default_img, t1.sku_title, t1.sku_subtitle, t1.price, t1.sale_count,
            t2.name AS catalog_name, t3.name AS brand_name
        FROM
            pms_sku_info AS t1
            LEFT JOIN pms_category AS t2 ON t1.catalog_id = t2.cat_id
            LEFT JOIN pms_brand AS t3 ON t1.brand_id = t3.brand_id
            LEFT JOIN pms_sku_images AS t4 ON t1.sku_id = t4.sku_id AND t4.default_img = 1
        <if test="skuQueryDTO != null">
            <where>
                <if test="skuQueryDTO.catalogId != null">
                    t1.catalog_id = #{spuQueryDTO.catalogId}
                </if>
                <if test="skuQueryDTO.brandId != null">
                    AND t1.brand_id = #{spuQueryDTO.brandId}
                </if>
                <if test="skuQueryDTO.minPrice != null">
                    AND t1.price &gt;= #{spuQueryDTO.minPrice}
                </if>
                <if test="skuQueryDTO.maxPrice != null">
                    AND t1.price &lt;= #{spuQueryDTO.maxPrice}
                </if>
                <if test="skuQueryDTO.keyword != null and skuQueryDTO.keyword != ''">
                    AND (
                        t1.sku_name LIKE CONCAT('%', #{skuQueryDTO.keyword}, '%') OR
                        t1.sku_title LIKE CONCAT('%', #{skuQueryDTO.keyword}, '%') OR
                        t1.sku_subtitle LIKE CONCAT('%', #{skuQueryDTO.keyword}, '%')
                    )
                </if>
            </where>
        </if>
    </select>


</mapper>
